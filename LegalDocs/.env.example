# Copy this to .env and fill in values if needed
# cp .env.example .env

# No frontend env vars are required for the app to run —
# all AI/ML calls go through the FastAPI backend.

# (These legacy keys are no longer used — the backend handles all LLM calls)
VITE_OPENROUTER_API_KEY=
VITE_GEMINI_API_KEY=
